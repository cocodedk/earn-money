#!/bin/sh
set -eu
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
[ -f "$ROOT/.env" ] && . "$ROOT/.env"
[ -x "$ROOT/.venv/bin/python" ] || { echo "submit: .venv missing" >&2; exit 1; }
exec "$ROOT/.venv/bin/python" -m earn_money.triage.submit --root "$ROOT" "$@"
