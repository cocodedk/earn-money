# Slice 19-C — runner + redaction + persistence

Redaction is wired BEFORE persistence in this single slice (codex Stage 5 finding: never persist raw error excerpts). No intermediate identity-passthrough commit.

1. `test_runner.py` failing → runner signature `run(scan_run: ScanRun, target_run: ScanTargetRun) -> None` mirroring 1.16/1.17. Candidate URLs come from `target.base_url` plus any URLs seeded by prior runners' evidence (full crawler integration deferred — see `out-of-scope.md`). Same-origin redirect policy (one hop max, stay on origin). Fetches with the per-stub bounded GET client (1.16/1.17 `fetcher.py` pattern; `_shared/http.py` lift deferred), classifies, **redacts via `redact.py` BEFORE persistence**, writes one `Evidence` row per candidate + one `Finding` per high/medium verdict.
2. Findings: `category=sql_orm_errors.<family>` (postgres / mysql / sqlalchemy / etc.), `data={signal_kind, framework_hints[], error_excerpt_redacted, disclosed_schema_terms[], severity}` (per spec 1.19 §typed-finding-shape: safe table/column/model names extracted from signature named-groups, included only when visible in evidence). `confidence` from the ladder. **Status vocabulary**: new finding → `candidate`; re-detection with matching dedup key → `confirmed`; disappearance on subsequent scan → `stale`. **Dedup key**: `(target_id, signal_kind, fingerprint(error_excerpt_redacted))`. **Severity**: from classifier.
3. Negative tests added to `test_runner.py`: redirect to login/WAF page → no Finding; timeout → no Finding + event logged; TLS handshake error → no Finding + event logged; re-run with same evidence → `confirmed` status flip (no duplicate row); re-run after fix → `stale` status flip.
4. /simplify round 1, commit. Commit subject: `feat(stubs): 1.19 sql_orm_errors runner + redaction + persistence`.
