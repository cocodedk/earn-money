# 1. Goal

Create a local Docker Compose setup where I can:

- start the whole app with one command
- open the React dashboard
- create projects
- create targets
- start a scan run
- pause a scan run
- stop a scan run
- view scan status per target
- view live scan events
- view findings later
- view evidence later
- run one stub at a time

The first real stub will be:

```text
1.1 framework-detection
```

But do not implement it yet.

Only create the structure that can run it later.
