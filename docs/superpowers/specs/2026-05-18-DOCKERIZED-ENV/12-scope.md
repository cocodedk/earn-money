# 12. Keep it small

Do not overbuild.

Do not add Kubernetes.

Do not add WebSockets yet.

Do not add authentication yet unless already present.

Do not add real vulnerability checks yet.

Build the platform skeleton first.

The next task will add stub:

```text
1.1 framework-detection
```
