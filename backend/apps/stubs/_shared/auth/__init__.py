"""Shared primitives for Phase 2 authentication stubs.

Modules:
* `identifiers` — synthetic invalid identifiers (RFC-6761 emails,
  scanner-prefixed usernames).

Future Phase 2 slices add: `forms`, `requests`, `normalize`,
`endpoints`, `safety`.
"""
