"""Stub 1.13 — security.txt public-metadata extraction.

Importing this package fires `@register("1.13")` on `run()` via runner.py.
The runner import is deferred until the runner module lands so partial
TDD slices (parser, classify, fetcher) can be tested in isolation.
"""
from __future__ import annotations
