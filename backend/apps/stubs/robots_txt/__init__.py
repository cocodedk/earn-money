"""Stub 1.11 — robots.txt public-metadata extraction.

Importing this package fires `@register("1.11")` on `run()` via runner.py.
The runner import is deferred until the runner module lands so partial
TDD slices (parser, signals, classify, fetcher) can be tested in isolation.
"""
from __future__ import annotations
